## 1. 后端公网 URL 配置

- [x] 1.1 先补充配置单元测试，覆盖 Web 模式缺失、非法及合法 `GITHUB_APP_PUBLIC_BASE_URL`，确认测试按预期失败后实现严格解析与规范化。
- [x] 1.2 补充 manifest 测试，证明 callback/setup URL 只使用配置值且不受请求头影响，并同步更新部署文档中的必填变量说明。

## 2. 前端默认入口

- [x] 2.1 先补充入口路由测试，覆盖已就绪、未安装、无已接入仓库和请求失败场景，确认测试按预期失败。
- [x] 2.2 实现共享入口判定组件，将根路径、登录成功和未知路由统一接入判定，同时保留 Agents 等显式路由。

## 3. 验证

- [x] 3.1 运行后端相关测试与构建、前端单元测试与构建，并核对 OpenSpec 验收场景与实现一致。
